{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Term Class Map Load Input",
  "description": "Input schema for forecast.term_class_map.load capability - loads term classification map",
  "type": "object",
  "required": ["a2_phase_path"],
  "properties": {
    "a2_phase_path": {
      "type": "string",
      "description": "Path to a2_phase JSON artifact containing term profiles",
      "minLength": 1
    },
    "seed": {
      "type": ["integer", "null"],
      "description": "Optional deterministic seed (unused but kept for consistency)"
    }
  },
  "additionalProperties": false
}
