{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Conspiracy Apply Horizon Cap Input",
  "description": "Input schema for conspiracy.policy.apply_cap capability",
  "type": "object",
  "required": ["policy_cap", "csp_cap"],
  "properties": {
    "policy_cap": {
      "type": ["string", "null"],
      "description": "Policy-based horizon cap (days, weeks, months, years) or null"
    },
    "csp_cap": {
      "type": ["string", "null"],
      "description": "CSP-based horizon cap or null"
    }
  },
  "additionalProperties": false
}
