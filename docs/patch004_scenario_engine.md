# PATCH-004 Sandbox Scenario Engine v1

Generates six deterministic sandbox scenarios, converts each to a repair manifest,
runs no-op execution, binds receipts, ranks outcomes deterministically, and emits
`ScenarioBatchReport.v1` with no execution authority.
