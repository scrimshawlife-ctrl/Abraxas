# TODO Audit — 2026-03-27

## Scope
Repository-wide TODO scan (excluding `.git/`, `vendor/`, and `package-lock.json`).

## Commands

```bash
rg -n --hidden --glob '!.git' --glob '!vendor/**' --glob '!package-lock.json' '\\bTODO\\b' /workspace/Abraxas
rg -n --hidden --glob '!.git' --glob '!vendor/**' --glob '!package-lock.json' 'TODO:' /workspace/Abraxas
rg -n --hidden --glob '!.git' --glob '!vendor/**' --glob '!package-lock.json' --glob '!docs/artifacts/todo_audit_2026-03-27.md' '\\bTODO\\b' /workspace/Abraxas
```

## Findings

### Actionable code TODOs
- None open in runtime code after follow-up remediation.
- Resolved marker: `abraxas/governance/rune_registry_gate.py:304`
  - Previous marker: `description: "TODO: add description"`
  - Replacement: `description: "Autogenerated scaffold; replace with domain-specific description."`

### Documentation / planning TODOs
1. `CLAUDE.md:151` — TODO section header.
2. `CLAUDE.md:208` — instruction referencing TODO/FIXME/XXX scan.
3. `docs/work_sessions/abx_runes_session_2026_01_07.md:472` — historical note (`TODO list updated`).

### Tooling references (non-action TODO markers)
- None (scanner wording normalized to avoid uppercase TODO token noise).

## Summary
- Baseline TODO mentions (initial scan): **7**
- Current TODO mentions after runtime remediation + scanner token cleanup (excluding this audit file): **4**
- Actionable in runtime code: **0** (1 resolved in follow-up)
