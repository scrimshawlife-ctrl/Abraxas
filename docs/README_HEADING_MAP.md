# README heading map

Applied on `main` 2026-09-04. Front-door headings that looked like product versions are **local module tags** in this runtime repo. They do not name or promote `Abraxas-v2.0`.

| Heading on main | Meaning |
|---|---|
| `## Rune layer (local module)` | Was `## v2.0.1 — Rune Layer`. Tokens `v2.0.1` remain in body text as module tags. |
| `## Adaptive sandbox (local module)` | Was `## v2.0.5 — Governed Adaptive Sandbox`. Tokens `v2.0.5` remain in body text as module tags. |

Start Here includes [`SIBLING_REPOS.md`](SIBLING_REPOS.md) and this map.

Doctrine/candidate architecture stays in [`scrimshawlife-ctrl/Abraxas-v2.0`](https://github.com/scrimshawlife-ctrl/Abraxas-v2.0).
