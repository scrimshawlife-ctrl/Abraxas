# README heading map

Front-door README headings that look like product versions are **local module tags** in this runtime repo. They do not name or promote `Abraxas-v2.0`.

| README heading (current on main) | Read as |
|---|---|
| `## v2.0.1 — Rune Layer` | Rune layer (local module). Tokens `v2.0.1` stay in body text. |
| `## v2.0.5 — Governed Adaptive Sandbox` | Adaptive sandbox (local module). Tokens `v2.0.5` stay in body text. |

Start Here should include [`SIBLING_REPOS.md`](SIBLING_REPOS.md).

A full README rewrite that applies those heading strings is staged for operator apply; this file binds the reading rule even before that rewrite merges.
