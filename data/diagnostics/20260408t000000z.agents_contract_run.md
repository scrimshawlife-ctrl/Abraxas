# AGENTS.md Contract Run — 2026-04-08

## objective
Execute repository governance checks aligned with `AGENTS.md` and capture deterministic evidence.

## scope and lane
- scope: repository-level governance and test-surface verification
- lane: `forecast-active` (validation-only; no authority mutation)

## subsystem id
- `oracle_signal_layer_v2` (default subsystem used by guardrail targets)

## change class
- `evidence_capture`

## proof targets
- preflight eligibility for subsystem
- registry and proof policy consistency
- governance lint and release-readiness
- continuity drift and repo-status snapshots
- baseline `pytest -q` collection viability

## tests and validation commands
1. `pytest -q`
   - result: `blocked`
   - reason: missing dependency `jinja2` during collection (`ModuleNotFoundError` in `tests/test_gate_stack_v0.py`).
2. `make repo-guardrails`
   - result: `candidate`
   - evidence:
     - preflight: `ELIGIBLE`
     - registry-check: `OK`
     - proof-check: `OK`
     - governance-lint: `OK`
     - release-readiness: `READY`
     - continuity-drift: `stable`
     - repo-status: counts `{active: 4, gated: 2, blocked: 2}`

## declared risks and stop conditions
- unresolved risk: Python env missing `jinja2`; full test proof remains `partial` until dependency is installed and `pytest -q` rerun.
- no stop condition triggered for governance scripts executed in this run.

## status label
`partial`
